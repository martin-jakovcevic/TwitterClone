<h1> <%= @tweet.title %> </h1>

<p> <%= @tweet.body %> </p>

<h2> <%= link_to "Edit", edit_tweet_path(@tweet) %> </h2>

<h2> <%= link_to "Back", root_path, data: { confirm: "Are you satisfied with your Tweet and want to go back ? "} %> <h2>

<h2> <%= link_to "Delete",tweet_path(@tweet), method: :delete, data: { confirm: "Are you sure you want to delete this tweet ? " } %> </h2>
