<ul>
  <% @tweets.each do |tweet| %>
  <li>
    <%= link_to tweet.title, tweet_path(tweet) %>
    <%= link_to "edit", edit_tweet_path(tweet) %>
    <%= link_to "Delete",tweet_path(tweet), method: :delete, data: { confirm: "Are you sure you want to delete this tweet ? " } %>
  </li>
  <% end %>
</ul>

